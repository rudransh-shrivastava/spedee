{
  "name": "spedee",
  "version": "1.0.0",
  "description": "spedee",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "concurrently \"npm run start --prefix backend\" \"npm run start --prefix backend\"",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "format": "npm run format --prefix backend && npm run format --prefix frontend"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
